pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout your Git repository
                checkout scm
            }
        }
        
        stage('Build and Deploy') {
            steps {
                script {
                    // docker-compose
                  sh  'docker-compose up -d'
                }
            }
        }
    }
}
